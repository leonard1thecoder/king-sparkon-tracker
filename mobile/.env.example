# Copy to .env (local only, never commit secrets).
# Mobile talks directly to the Spring Boot backend — no Next.js proxy.
EXPO_PUBLIC_BACKEND_URL=http://localhost:8080
EXPO_PUBLIC_APP_URL=http://localhost:8081
