import { KingSparkonLoader } from "@/components/ui/KingSparkonLoader";

export default function Loading() {
  return (
    <KingSparkonLoader
      label="King Sparkon platform loading"
      message="Preparing the full platform experience."
    />
  );
}
